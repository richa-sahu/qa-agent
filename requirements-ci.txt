# requirements-ci.txt
# Minimal dependencies for CI — generate + run only
# No LangChain/Ollama needed (healing is local only)
PyYAML==6.0.2
requests==2.32.3
pytest==8.3.3
pytest-json-report==1.5.0
rich==13.8.1
python-dotenv==1.0.1
jinja2==3.1.4
openapi-spec-validator==0.7.1